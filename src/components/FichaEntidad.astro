---
import type { Entidad, TipoNodo } from "../lib/grafo";
import { rutaEntidad, rutaMateria, rutaRelato } from "../lib/rutas";

interface Props {
	entidad: Entidad;
	materiaSlug: string;
	nodos: Map<string, { nombre: string; kind: TipoNodo }>;
	relatosParticipante: Array<{ id: string; nombre: string }>;
}
const { entidad, materiaSlug, nodos, relatosParticipante } = Astro.props;

function rutaNodo(id: string): string {
	return nodos.get(id)?.kind === "relato"
		? rutaRelato(materiaSlug, id)
		: rutaEntidad(materiaSlug, id);
}
---

<article>
  <h1>{entidad.nombre}</h1>
  <p class="tipo">{entidad.tipo}</p>
  <p>{entidad.resumen}</p>

  {
    entidad.etiquetas?.length ? (
      <ul class="etiquetas">
        {entidad.etiquetas.map((et) => (
          <li>{et}</li>
        ))}
      </ul>
    ) : null
  }

  {
    entidad.atributos ? (
      <dl>
        {Object.entries(entidad.atributos).map(([clave, valor]) => (
          <div>
            <dt>{clave}</dt>
            <dd>{String(valor)}</dd>
          </div>
        ))}
      </dl>
    ) : null
  }

  {
    entidad.relaciones.length > 0 && (
      <section>
        <h2>Relaciones</h2>
        <ul>
          {entidad.relaciones.map((r) => (
            <li>
              {r.tipo.replaceAll('_', ' ')}:{' '}
              <a href={rutaNodo(r.destino)}>{nodos.get(r.destino)?.nombre ?? r.destino}</a>
            </li>
          ))}
        </ul>
      </section>
    )
  }

  {
    relatosParticipante.length > 0 && (
      <section>
        <h2>Relatos</h2>
        <ul>
          {relatosParticipante.map((r) => (
            <li>
              <a href={rutaRelato(materiaSlug, r.id)}>{r.nombre}</a>
            </li>
          ))}
        </ul>
      </section>
    )
  }

  <p><a href={rutaMateria(materiaSlug)}>&larr; volver</a></p>
</article>
